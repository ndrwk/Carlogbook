<?xml version="1.0" encoding="utf-8"?>
<manifest package="ak.logbook"
    android:installLocation="auto"
    android:versionCode="1"
    android:versionName="1.0" xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="18" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity
            android:name="ak.logbook.Main"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ak.logbook.CarActivity">
            android:windowSoftInputMode="adjustPan"
        </activity>
        <activity android:name="ak.logbook.CategoryActivity"></activity>
        <activity android:name="ak.logbook.CurrencyActivity"></activity>
        <activity android:name="ak.logbook.PartActivity"></activity>
        <activity android:name="ak.logbook.ProviderActivity"></activity>
        <activity android:name="ak.logbook.EventActivity">
            android:windowSoftInputMode="adjustPan"
        </activity>

    </application>

</manifest>